spring.application.name=QLD Service Intelligence

spring.datasource.url=jdbc:postgresql://localhost:5432/qld_service_intelligence
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# Development only: update the schema when entities are introduced.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
